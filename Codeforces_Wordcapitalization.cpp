#include<bits/stdc++.h>

using namespace std;

int main() {
    string line;

    cin >> line;

    if(line[0] >= 'a' && line[0] <= 'z') {
        line[0] -= 32;
    }

    cout << line << endl;












    return 0;
}
